<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Horario</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            color: #212529;
            padding-top: 20px;
        }
        h1 {
            margin-bottom: 20px;
        }
        .info-section {
            margin-bottom: 20px;
        }
        .table-container {
            margin-top: 20px;
        }
        .table {
            background-color: white;
            border-radius: 8px;
            overflow: hidden;
        }
        .table th {
            background-color: #e9ecef;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="text-center">Horario para el día <span class="text-primary">{{dia}}</span></h1>
        <div class="info-section text-center">
            <p><strong>Diagrama:</strong> {{diagrama}} <strong>Franco:</strong> {{franco}} </p>
            
        </div>
        <div class="text-center">
            <p><strong>Tomas a las:</strong> {{horarioTomas}}</p>
            <p><strong>Dejas a las:</strong> {{horarioDejas}}</p>
        </div>
        <div class="table-container">
            <h3 class="text-center">Detalles de los trenes</h3>
            <div class="table-responsive">
                <table class="table table-bordered table-striped text-center">
                    <thead class="table-light">
                        <tr>
                            <th>Tren</th>
                            <th>Salida</th>
                            <th>Origen</th>
                            <th>Destino</th>
                            <th>Llegada</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each tabla}}
                        <tr>
                            <td>{{tren}}</td>
                            <td>{{salida}}</td>
                            <td>{{origen}}</td>
                            <td>{{destino}}</td>
                            <td>{{llegada}}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
        <div class="footer" style="text-align: center; margin-top: 20px; font-size: 14px; color: #555;">
    <p>Esta app fue realizada por Pagano Germán🚀</p>
</div>

    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>



